# WI-24 — 发布与分发

## 意图

定义多平台构建、校验和、SBOM、签名和 provenance 证据。

## 验收

发布自动化必须发布证据，不能声称 core 自我证明。

## 证据

release workflow 和文档

## 结果

状态：**Implemented，受保护的发布执行待完成**。矩阵覆盖 macOS arm64/x86_64、Linux arm64/x86_64 和 Windows x86_64；已定义校验和、SPDX SBOM、provenance attestation 和可选 cosign 签名。最终回执需要受保护 CI 凭据和 release tag。
