# WI-15 — 生命周期写边界

## 意图

把仓库写入限制在 .ai facts、evidence、decisions 和 knowledge。

## 验收

不得向仓库复制 runtime 源码或安装器文件。

## 证据

attach/doctor/lifecycle 测试，包括 fail-closed 权限默认值和关闭决策

## 结果

状态：**Implemented**。attach/doctor/lifecycle 测试覆盖轻量仓库写入、缺失权限默认值和显式人类关闭决策。
