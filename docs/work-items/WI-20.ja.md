# WI-20 — MCP Read Surface

## 意図

status、list/get、blockers/actions、knowledge、evidence、observe、preflight を公開する。

## 受入れ

MCP は repository/core service に委譲し、path escape を拒否する。

## 証拠

MCP RPC/security テスト

## 結果

状態：**Implemented**。repository binding のない MCP tool call は fail closed し、bound read tool は path escape を拒否する。
