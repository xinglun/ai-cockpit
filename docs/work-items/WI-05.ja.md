# WI-05 — Git Repository Snapshot

## Intent

Create one immutable snapshot with status, diff, tree, and dependency fingerprints.

## 受入れ

Snapshot reports deterministic paths, digests, file/hash counts, and bounded git calls.

## Evidence

cockpit-git repository tests; inspect telemetry

## Outcome

状態：**Implemented**。実装証拠に基づいて更新し、partial のフェーズを green と主張しない。
