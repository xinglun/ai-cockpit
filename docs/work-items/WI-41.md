---
author: AI Cockpit maintainers
title: "WI-41 — Reference source parity and reader entry"
description: "Compare the Rust runtime with the reference source and correct the public entry route."
audience:
  - maintainer
  - reviewer
status: current
authority: canonical
lastVerifiedBy: wi-41-reference-parity
capabilityClaims:
  - reference_parity
---

# WI-41 — Reference source parity and reader entry

## Intent and goal

Record an evidence-backed comparison with `spirex-ds-dev/ai-cockpit-template`
without importing V1 runtime state, then make the Rust project's public entry
route usable for a first-time adopter.

## Scope

- compare the reference tree, user journey, capability index, and release/
  upgrade boundaries with the current Rust implementation;
- add the trilingual parity matrix and explicit follow-up boundaries;
- cross-link the root README language variants and add a runnable 30-second
  attach/status route;
- keep `cockpit.toml` as TOML and preserve the explicit V1 non-install boundary.

## Acceptance

1. The parity page names the reference and Rust commits and distinguishes
   implemented, partial, intentionally absent, and follow-up capabilities.
2. English, Chinese, and Japanese root READMEs link to one another and expose a
   concrete attach/status first step.
3. No claim is added without current code, test, or documentation evidence.
4. No V1 file, runtime, global Agent setting, or repository schema is copied.

## Evidence

See `docs/reference/reference-parity*`, the three root README files, and the
installed Runtime verification receipt for this Work Item.

## Outcome

Implementation is complete after the parity document and reader entry are
verified. Upgrade behavior and internal documentation cleanup remain WI-43 and
WI-42 respectively.
