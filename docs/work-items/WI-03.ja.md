# WI-03 — Rust Workspace and CLI

## Intent

Implement the controlled Rust workspace and single binary command surface.

## 受入れ

Workspace builds; inspect is read-only; CLI tests pass.

## Evidence

cargo test --workspace; CLI integration tests

## Outcome

状態：**Implemented**。実装証拠に基づいて更新し、partial のフェーズを green と主張しない。
