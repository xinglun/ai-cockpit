# WI-03 — Rust ワークスペースと CLI

## 意図

制御された Rust ワークスペースと単一バイナリのコマンド面を実装する。

## 受入れ

ワークスペースがビルドでき、inspect は読み取り専用で、CLI テストが通る。

## 証拠

cargo test --workspace；CLI 統合テスト

## 結果

状態：**Implemented**。実装証拠で更新し、段階が partial の場合は green gate を主張しない。
