# WI-21 — MCP 验证面

## 意图

提供有界验证，但不增加写入型生命周期工具。

## 验收

命令在目标仓库 cwd 运行，使用 allowlist，并可记录 evidence。

## 证据

MCP verify 路径/安全测试

## 结果

状态：**Implemented**。CLI 和 MCP 验证构建 protected graph，在仓库 cwd 执行，并记录绑定快照的证据。
