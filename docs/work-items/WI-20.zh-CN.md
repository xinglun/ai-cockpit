# WI-20 — MCP 只读面

## 意图

提供 status、list/get、blockers/actions、knowledge、evidence、observe 和 preflight。

## 验收

MCP 委托给 repository/core 服务，并拒绝路径逃逸。

## 证据

MCP RPC/安全测试

## 结果

状态：**Implemented**。未绑定仓库的 MCP 工具调用 fail closed，绑定仓库的只读工具拒绝路径逃逸。
