# WI-21 — MCP Verify Surface

## Intent

Expose bounded verification without adding write lifecycle tools.

## Acceptance

Commands run in target repository cwd, use an allowlist, and can record evidence.

## Evidence

MCP verify path/security tests

## Outcome

Status: **Implemented**. CLI and MCP verification build a protected graph, run in the repository cwd, and record snapshot-bound evidence.
