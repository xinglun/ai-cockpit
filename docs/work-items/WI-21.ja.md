# WI-21 — MCP Verify Surface

## 意図

write lifecycle tool を追加せず bounded verification を公開する。

## 受入れ

command は対象 repository の cwd で実行し、allowlist を使い、evidence を記録できる。

## 証拠

MCP verify path/security テスト

## 結果

状態：**Implemented**。CLI と MCP verification は protected graph を構築し、repository cwd で実行し、snapshot-bound evidence を記録する。
