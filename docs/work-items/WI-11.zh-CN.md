# WI-11 — Preflight

## 意图

在执行变更前检查 protocol 和 contract 是否新鲜。

## 验收

缺失证据为 yellow；上下文过期为 red；输出为结构化数据。

## 证据

CLI/MCP preflight 测试

## 结果

状态：**Implemented**。Preflight 区分缺失、矛盾、过期和匹配的验证证据；CLI 测试覆盖从 yellow 到 green 的转换。
