# WI-05 — Git 仓库快照

## 意图

创建包含状态、diff、树和依赖指纹的一次性不可变快照。

## 验收

快照报告确定性路径、digest、文件/哈希计数和有界 Git 调用。

## 证据

cockpit-git repository 测试；inspect telemetry

## 结果

状态：**Implemented**。快照测试覆盖确定性 digest、有界 Git 调用，以及依赖路径重叠时只哈希一次。
