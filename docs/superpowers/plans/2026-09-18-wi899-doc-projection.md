# WI-899 Documentation Projection Repair Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Repair the terminal documentation projection that currently keeps closed WI-899 conditional and thereby restore the four affected repository gates.

**Architecture:** Keep Runtime-generated `.ai` archive, verification, finalization, and close bytes immutable. Update only the three maintained WI-899 language pages and their three parity rows; the self-registration pages and rows for this bounded promotion Work Item provide the finite projection boundary required by the Runtime.

**Tech Stack:** Markdown, the installed AI Cockpit Runtime v0.2.98, and the repository's Python/shell documentation and CI gate checks.

**Spec:** `docs/work-items/WI-903-wi899-doc-promotion.md`

## Global Constraints

- Do not edit `.ai/**` generated lifecycle or release evidence.
- Do not modify object repositories under `/Users/sei-rinn/dev/workspace_rust/`.
- Do not create, retag, or publish a release.
- Preserve all existing WI-899 evidence references and the three-language parity order.
- Use the explicit repository path with Runtime v0.2.98 for lifecycle commands.

---

### Task 1: Establish and checkpoint the bounded documentation Work Item

**Files:**
- Read: `docs/work-items/WI-903-wi899-doc-promotion.md`
- Read: `docs/reference/reference-parity.md`
- Read: `tests/docs/work_item_status_consistency.py`

- [ ] **Step 1: Run the Runtime preflight after self-registration exists**

Run `ai-cockpit preflight --repo "$PWD" --contract .ai/work-items/active/WI-903-wi899-doc-promotion.contract.json` and expect a green decision with no blockers. If the Runtime reports a blocker, preserve the review and do not edit outside the declared scope.

- [ ] **Step 2: Record the before-edit checkpoint**

Run `ai-cockpit checkpoint --repo "$PWD" --id WI-903-wi899-doc-promotion` and confirm the summary reports one checkpoint and the declared base `fc51bd2723d468d82d3cade843a2a7cca14c8873`.

### Task 2: Promote WI-899's terminal projection

**Files:**
- Modify: `docs/work-items/WI-899-release-v0-2-98.md`
- Modify: `docs/work-items/WI-899-release-v0-2-98.zh-CN.md`
- Modify: `docs/work-items/WI-899-release-v0-2-98.ja.md`
- Modify: `docs/reference/reference-parity.md`
- Modify: `docs/reference/reference-parity.zh-CN.md`
- Modify: `docs/reference/reference-parity.ja.md`

- [ ] **Step 1: Change only the terminal projection fields**

Change each WI-899 page's `status: in_progress` to `status: implemented`; keep its `workItemId`, `lastVerifiedBy`, body, and all archive/verification/finalization/close references byte-for-byte otherwise unchanged.

- [ ] **Step 2: Replace only the conditional parity vocabulary**

Change the English row to `Implemented`, the Simplified Chinese row to `已实现`, and the Japanese row to `Implemented`; retain the same Work Item link and evidence references.

- [ ] **Step 3: Inspect the diff**

Run `git diff --check` and `git diff -- docs/work-items/WI-899-release-v0-2-98* docs/reference/reference-parity*`. Expected: only six projection files changed and no `.ai` path changed.

### Task 3: Run deterministic gates before expensive verification

**Files:**
- Test: `tests/docs/documentation_acceptance.sh`
- Test: `tests/docs/work_item_status_consistency.py`
- Test: `tests/ci/repository_gate_manifest_test.py`

- [ ] **Step 1: Run the focused documentation checks**

Run `python3 tests/docs/work_item_status_consistency.py --repo "$PWD"`, `bash tests/docs/parity_status_check.sh "$PWD" /tmp/wi903-parity.json`, `bash tests/docs/documentation_acceptance.sh --repo "$PWD"`, and `python3 tests/ci/repository_gate_manifest_test.py`. Expected: all commands exit 0 without starting a Rust verification subprocess.

- [ ] **Step 2: Record verification evidence through Runtime**

Use `ai-cockpit verify --repo "$PWD" --work-item WI-903-wi899-doc-promotion` with the Contract-declared deterministic documentation commands, preserving each command, exit code, bounded output, and the observed subprocess count.

- [ ] **Step 3: Finish and obtain the human Outcome**

Run `ai-cockpit finish --repo "$PWD" --id WI-903-wi899-doc-promotion`, then query `ai-cockpit work-item outcome --repo "$PWD" --id WI-903-wi899-doc-promotion --view full`. The Outcome must state that this is a documentation-gate repair, v0.2.98 was not republished, and interface SSOT, actual conversation display, and performance improvement remain outside this WI.

### Task 4: Archive, close, and validate the mainline fix

**Files:**
- Generated by Runtime: `.ai/work-items/archive/**`, `.ai/evidence/**`, `.ai/decisions/**`

- [ ] **Step 1: Archive and close only after green verification**

Run `ai-cockpit archive --repo "$PWD" --id WI-903-wi899-doc-promotion`, deliver the complete human Outcome, and run `ai-cockpit close --repo "$PWD" --id WI-903-wi899-doc-promotion`.

- [ ] **Step 2: Run the post-close projection check**

Run `python3 tests/docs/promote_closed_work_item.py --repo "$PWD" --check-all`. Expected: no stale WI-899 projection and no successor requirement for the bounded WI-903 self-projection.

- [ ] **Step 3: Review, merge, and re-check main**

Commit the scoped documentation and Runtime evidence, push the dedicated branch with `gh` using the local `xinglun` account, open a reviewed PR, and merge only after its hosted checks pass. Synchronize `main`, rerun the four affected gates, and report the exact PR, CI run, release preservation, and cleanup state. Do not publish another version.
