# Repository evolution fixtures

`cockpit-repository` tests cover L0 content changes, L1 new tests with automatic
absorption, L2 new frameworks with a confirmation proposal, and L3 governance
paths requiring human decision. Evolution is reported as facts plus a profile
update proposal; it never silently changes a mandatory gate.
